import { ICommand } from "wokcommands";
// import axios from "axios";

export default {
  category: "s",
  description: "S",
  slash: "both",
  testOnly: true,

  callback: ({ client, text }) => {},
} as ICommand;
